# 프로젝트 작업 원칙

이 문서는 `D:\ai\my_harness`의 개발 규칙과 현재 MSA 프로젝트 규칙을 합친 작업 기준입니다.

## 1. 기본 방향
- 요구사항을 먼저 좁게 정의합니다.
- 필요한 것만 만듭니다.
- 불필요한 추상화, 범용 구조, 과한 확장성은 만들지 않습니다.
- 변경은 요청과 직접 연결되어야 합니다.

## 2. 변경 원칙
- 구조 변경과 기능 변경은 가능하면 분리합니다.
- 기존에 잘 동작하는 코드는 함부로 손대지 않습니다.
- 새 기능은 가장 단순한 형태부터 추가합니다.
- 새로운 의존성은 꼭 필요할 때만 추가합니다.

## 3. 검증 원칙
- 변경이 있으면 검증 기준도 함께 둡니다.
- 테스트 가능한 코드는 성공/실패 케이스를 구분합니다.
- UI나 문서 작업은 목표 달성 여부를 기준으로 확인합니다.
- 한 번에 너무 많은 변화를 넣지 않습니다.

## 4. MSA 개발 규칙
- 기본 패키지는 `com.msa`를 사용합니다.
- 조회 메서드는 `select*`를 사용합니다.
- 저장 메서드는 `insert`를 사용합니다.
- 코드 구조는 `controller`, `service`, `mapper`, `xml`, `vo`를 기본으로 합니다.
- DTO는 요청이 없으면 추가하지 않습니다.
- 서비스별 DB 설정을 따로 둡니다.
- 기본 DB는 H2입니다.
- `gateway-service`가 백엔드 진입점입니다.
- 모든 서비스는 `X-Trace-Id`를 전파하고 trace를 기록합니다.
- `trace-service`에 모든 trace를 중앙 저장합니다.

## 5. 서비스 경계
- 내부 호출은 플랫폼 내부 서비스 간 호출입니다.
- 내부 동기 호출은 Feign을 우선 사용합니다.
- 외부 호출은 플랫폼 바깥 시스템 호출입니다.
- 외부 호출은 RestClient 또는 WebClient를 사용합니다.
- `hr-service`는 다른 시스템과 독립된 계약으로 취급합니다.

## 6. 인증 규칙
- JWT 발급은 `auth-service`가 담당합니다.
- JWT 검증과 차단은 `gateway-service`가 담당합니다.
- 회원가입과 로그인은 H2 메모리 DB를 사용합니다.
- 서비스 재시작 시 H2 데이터는 사라집니다.

## 7. 테스트 규칙
- `test-service`는 백엔드 통합 테스트용 UI입니다.
- 모든 백엔드 호출은 `gateway-service`를 경유합니다.
- `test-service`는 DB를 사용하지 않습니다.

## 8. 작업 순서
1. 요구사항 확인
2. 현재 구조 확인
3. 최소 범위 수정
4. 필요 시 문서 동기화
5. 검증
